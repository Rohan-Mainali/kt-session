apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-cm
data:
  token: "jwt-token-updated"
  ca.crt: "certificate-signed"
  game.properties: |
    enemy.types=aliens,monsters
    player.maximum-lives=5
  user-interface.properties: |
    color.good=purple
    color.bad=yellow
    allow.textmode=true
